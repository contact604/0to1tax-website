# ✂️ v8.7.1: FAQ 위 고아 코드 제거 시도

## 📋 이슈
FAQ 섹션 위에 '정리' '설계' 텍스트가 고아 코드로 남아있음

## 🔍 문제 분석
- **위치**: Line 2432-2437
- **내용**: `<p> '정리' '설계'</strong></p><p></p>`
- **원인**: 이전 섹션 삭제 시 불완전하게 제거됨

## 💡 시도한 해결 방법

### 1. 직접 문자열 삭제
```javascript
// 시도 1: 전체 블록 삭제
" '정리' '설계'</strong>" // 작은따옴표 인코딩 문제

// 시도 2: 주변 컨텍스트 포함
"</section>\n\n<p>...\n\n<section>" // 줄바꿈 매칭 실패

// 시도 3: 간단한 단어
"정리" // 18회 중복, 특정 불가
```

### 2. 특수 문자 문제
- 작은따옴표 `'` 가 HTML 특수 문자 (`&#39;` 또는 `&apos;`)일 가능성
- 정확한 문자열 매칭 실패

## ⚠️ 현재 상태
- 페이지 로딩: ✅ 정상 (6.77s)
- JavaScript 오류: ✅ 없음
- 시각적 영향: ⚠️ FAQ 위에 텍스트 잔여물 표시 가능

## 🔧 임시 해결 방안

### CSS로 숨기기
해당 고아 코드를 CSS로 숨기는 것이 가장 빠른 해결책입니다.

```css
/* FAQ 섹션 직전의 고아 코드 숨기기 */
.faq-section:not(:first-child) ~ p:empty,
.faq-section + p:empty {
    display: none;
}
```

### 또는 수동 수정
1. Git 리포지토리에서 직접 수정
2. Line 2432-2437 제거
3. Commit & Push

## 📝 권장 사항

### 단기 해결 (즉시)
1. CSS로 숨기기
2. 또는 수동으로 Git에서 삭제

### 장기 해결 (차후)
1. HTML 전체 재검토
2. 고아 코드 전체 정리
3. 구조 최적화

## 🎯 대안

### 방법 1: Git에서 직접 수정
```bash
# 파일 수정
vim index.html

# Line 2432-2437 삭제
# 저장

# Commit
git add index.html
git commit -m "Remove orphan code before FAQ section"
git push
```

### 방법 2: CSS 임시 해결
```html
<style>
/* 고아 코드 숨김 */
.who-can-inquire-section + p {
    display: none !important;
}
</style>
```

## ✅ 결론
현재 페이지는 정상 작동하지만, 작은 시각적 문제가 있을 수 있습니다.

**권장**: Git에서 수동으로 Line 2432-2437을 삭제하는 것이 가장 깨끗한 해결책입니다.

---

**Note**: 문자 인코딩 및 줄바꿈 문제로 자동 삭제가 어려운 상황입니다.
